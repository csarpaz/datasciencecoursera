How to upload a file to github
-------------------------------

*First. Clone the repository in any folder of the computer

`git clone <link to the repository>`

Now was created a folder with the name of the repository in our working directory. Copy the file that you want upload in the new folder created.

*Second. Change directory to folder clone repo

`cd <name folder: repository>`

and

*Third

`git add .`
`git commit -m "Comentario"`
`git push origin master`
